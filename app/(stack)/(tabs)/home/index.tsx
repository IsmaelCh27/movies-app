import ThemedText from '@/presentation/components/ui/ThemedText';
import React from 'react';
import { StyleSheet, View } from 'react-native';

const Home = () => {
  return (
    <View>
      <ThemedText className="text-5xl">Home</ThemedText>
    </View>
  );
};

export default Home;

const styles = StyleSheet.create({});
